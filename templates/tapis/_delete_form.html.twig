<form method="post" action="{{ path('app_tapis_delete', {'id': tapi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tapi.id) }}">
    <button class="btn">Delete</button>
</form>
