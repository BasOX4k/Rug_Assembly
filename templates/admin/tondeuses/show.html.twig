{% extends 'base.html.twig' %}

{% block title %}Tondeuses{% endblock %}

{% block body %}
    <h1>Tondeuses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tondeus.id }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-danger" href="{{ path('app_tondeuses_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('app_tondeuses_edit', {'id': tondeus.id}) }}">edit</a>

    {{ include('admin/tondeuses/_delete_form.html.twig') }}
{% endblock %}
