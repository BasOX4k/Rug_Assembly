{% extends 'base.html.twig' %}

{% block title %}Tapis{% endblock %}

{% block body %}
    <h1>Tapis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tapi.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tapis_index') }}">back to list</a>

    <a href="{{ path('app_tapis_edit', {'id': tapi.id}) }}">edit</a>

    {{ include('admin/tapis/_delete_form.html.twig') }}
{% endblock %}
