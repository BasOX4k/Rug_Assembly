<form method="post" action="{{ path('app_pistolets_delete', {'id': pistolet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pistolet.id) }}">
    <button class="btn">Delete</button>
</form>
