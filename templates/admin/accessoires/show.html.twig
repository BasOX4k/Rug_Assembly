{% extends 'base.html.twig' %}

{% block title %}Accessoires{% endblock %}

{% block body %}
    <h1>Accessoires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accessoire.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accessoires_index') }}">back to list</a>

    <a href="{{ path('app_accessoires_edit', {'id': accessoire.id}) }}">edit</a>

    {{ include('admin/accessoires/_delete_form.html.twig') }}
{% endblock %}
