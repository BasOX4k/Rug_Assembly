{% extends 'base.html.twig' %}

{% block title %}Accessoires index{% endblock %}

{% block body %}
    <h1>Accessoires index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for accessoire in accessoires %}
            <tr>
                <td>{{ accessoire.id }}</td>
                <td>
                    <a href="{{ path('app_accessoires_show', {'id': accessoire.id}) }}">show</a>
                    <a href="{{ path('app_accessoires_edit', {'id': accessoire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_accessoires_new') }}">Create new</a>
{% endblock %}
